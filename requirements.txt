# SingNet pinned environment (MASTER_PLAN §8 / PLAN §6.1).
#
# These are the EXACT versions the CPU test suite (tests/, MASTER_PLAN G0) was
# validated against. They install and pass on CPU with no GPU and no dataset.
#
# torch note: the pin below resolves to the default PyPI wheel. For a smaller
# CPU-only install use the CPU index:
#     pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu
# The code is device-agnostic; AMP training paths only engage on CUDA.

# --- core (installed + test-validated here; import-required by singnet/) -----
torch==2.13.0
numpy==2.4.6
soundfile==0.14.0
PyYAML==6.0.1
pandas==3.0.3
matplotlib==3.11.0
tqdm==4.68.4
pytest==9.1.1
nbformat==5.10.4

# --- data-time extras (RUN LATER; NOT imported by the test suite) ------------
# Installed only on Colab/local at data-prep and final-eval time. The test suite
# guards every use with pytest.importorskip, so these staying uninstalled does
# not break `pytest`. Versions are resolved/frozen at data-prep time and the
# museval version is additionally recorded in the eval CSVs (§12 version drift).
#   musdb==0.4.2        # MUSDB18 STEMS parser + the frozen valid split list
#   stempeg==0.2.3      # STEMS (.mp4/AAC) decoding used by prepare_data.py
#   museval==0.4.1      # BSS-Eval v4 SDR — the clearly-labelled secondary metric

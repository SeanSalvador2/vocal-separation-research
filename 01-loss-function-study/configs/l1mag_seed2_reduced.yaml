# Sweep arm (MASTER_PLAN §3.2): l1mag, seed 2, REDUCED budget.
# Only `arm`/`seed` differ from base.yaml - the controlled-comparison invariant.
base: base.yaml
arm: l1mag
seed: 2
budget_name: reduced

# Sweep arm (MASTER_PLAN §3.2): l1mrstft, seed 1, REDUCED budget.
# Only `arm`/`seed` differ from base.yaml - the controlled-comparison invariant.
base: base.yaml
arm: l1mrstft
seed: 1
budget_name: reduced

# Sweep arm (MASTER_PLAN §3.2): sisdr, seed 0, REDUCED budget.
# Only `arm`/`seed` differ from base.yaml - the controlled-comparison invariant.
base: base.yaml
arm: sisdr
seed: 0
budget_name: reduced

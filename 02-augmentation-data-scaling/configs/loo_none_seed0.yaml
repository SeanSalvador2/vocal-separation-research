# LOO ablation (MASTER_PLAN §3.1): NO augmentation — the recipe floor. Seed 0.
# val(full) - val(none) = Δ_total, the total value of the recipe (a descriptive
# quantity with a CI, §2). Chunk sampling (random start) stays on everywhere.
base: base.yaml
seed: 0
augment:
  remix: false
  gain: false
  flip: false

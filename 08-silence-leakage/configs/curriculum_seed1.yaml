# Direction 08 arm (MASTER_PLAN §4.2 run matrix): curriculum sampling, seed 1.
base: base.yaml
arm: curriculum
loss: l1mag
seed: 1
budget_name: reduced
sampling:
  policy: curriculum
  floor_lambda: 0.1

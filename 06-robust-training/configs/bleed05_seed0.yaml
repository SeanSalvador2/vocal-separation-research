# Direction 06 arm (MASTER_PLAN §3.3 run matrix): ε=0.05 bleed, seed 0 (dose-response middle point).
base: base.yaml
arm: bleed05
loss: l1mag                # actual training loss (arm is the experimental id)
seed: 0
budget_name: reduced
corrupt:
  epsilon: 0.05          # ṽ = v + εa, ã = (1−ε)a on TRAIN targets only (§3.1)

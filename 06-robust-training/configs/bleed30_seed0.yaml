# Direction 06 arm (MASTER_PLAN §3.3 run matrix): ε=0.30 bleed, seed 0 (3-seed anchor cell).
base: base.yaml
arm: bleed30
loss: l1mag                # actual training loss (arm is the experimental id)
seed: 0
budget_name: reduced
corrupt:
  epsilon: 0.3          # ṽ = v + εa, ã = (1−ε)a on TRAIN targets only (§3.1)

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
.ruff_cache/
.mypy_cache/

# Training artefacts and data (never committed — MASTER_PLAN §4.1 license hygiene)
checkpoints/
*.pt
*.ckpt
*.onnx

# MUSDB / decoded audio shards (research-only, non-redistributable)
*.wav
*.mp4
*.stem.mp4
data/
shards/

# Results are committed selectively (registry.csv + eval CSVs). Local run
# outputs that are NOT part of the committed record:
01-loss-function-study/results/listening_kit/
01-loss-function-study/results/figures/

# OS / editor
.DS_Store
